@extends('layouts.tms')
@section('title','Audit log')
@section('content')
<div><p class="text-[10px] font-bold tracking-[.15em] text-blue-600 uppercase">Governance</p><h1 class="mt-1 text-2xl font-extrabold tracking-[-.045em] sm:text-3xl">Audit log</h1><p class="mt-1 text-xs text-slate-500">Append-only history of privileged and workflow actions.</p></div><section class="mt-6 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div class="hidden grid-cols-[.8fr_1.1fr_.7fr_1fr] gap-4 border-b border-slate-100 bg-slate-50 px-5 py-3 text-[8px] font-bold text-slate-400 uppercase md:grid"><span>Time</span><span>Actor</span><span>Action</span><span>Entity</span></div><div class="divide-y divide-slate-100">@forelse($logs as $log)<div class="grid gap-2 px-5 py-4 md:grid-cols-[.8fr_1.1fr_.7fr_1fr] md:items-center"><p class="text-[9px] text-slate-500">{{ $log->created_at->format('d M Y, g:i A') }}</p><p class="text-[10px] font-bold">{{ $log->actor?->name??'System' }}</p><span class="w-fit rounded-full bg-blue-50 px-2.5 py-1 text-[8px] font-bold text-blue-700">{{ str($log->action)->replace('.',' ')->title() }}</span><p class="text-[9px] text-slate-500">{{ str($log->entity_type)->title() }} @if($log->entity_id)#{{ $log->entity_id }}@endif</p></div>@empty<div class="py-16 text-center text-xs text-slate-400">No audit events yet.</div>@endforelse</div></section><div class="mt-4">{{ $logs->links() }}</div>
@endsection
