@extends('layouts.tms')
@section('title','Hospitals')
@section('content')
<div x-data="{ createOpen: false }"><div class="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p class="text-[10px] font-bold tracking-[.15em] text-blue-600 uppercase">Account directory</p><h1 class="mt-1 text-2xl font-extrabold tracking-[-.045em] sm:text-3xl">Hospitals</h1><p class="mt-1 text-xs text-slate-500">{{ $hospitals->total() }} accounts available in your scope.</p></div>@if($tmsUser->isRole('GM','AM'))<button @click="createOpen=true" class="inline-flex min-h-11 items-center justify-center gap-2 rounded-xl bg-blue-600 px-4 text-xs font-bold text-white"><i data-lucide="plus" class="size-4"></i>Create hospital</button>@endif</div>
<form class="mt-5 flex flex-col gap-2 rounded-2xl border border-slate-200 bg-white p-3 shadow-sm sm:flex-row"><label class="relative min-w-0 flex-1"><span class="sr-only">Search hospitals</span><i data-lucide="search" class="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-slate-400"></i><input name="search" value="{{ request('search') }}" class="h-10 w-full rounded-xl border-slate-200 pl-10 text-xs" placeholder="Search hospital…"></label><select name="business_unit_id" class="h-10 rounded-xl border-slate-200 text-xs"><option value="">All business units</option>@foreach($businessUnits as $unit)<option value="{{ $unit->id }}" @selected((int)request('business_unit_id')===$unit->id)>{{ $unit->name }}</option>@endforeach</select><button class="h-10 rounded-xl bg-slate-950 px-5 text-[10px] font-bold text-white">Apply</button></form>
<div class="mt-5 grid gap-3 sm:grid-cols-2 xl:grid-cols-3">@forelse($hospitals as $hospital)<article class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div class="flex items-start justify-between"><span class="grid size-10 place-items-center rounded-xl bg-blue-50 text-blue-700"><i data-lucide="building-2" class="size-4"></i></span><span class="rounded-full px-2.5 py-1 text-[7px] font-bold {{ $hospital->is_active?'bg-emerald-50 text-emerald-700':'bg-slate-100 text-slate-500' }}">{{ $hospital->is_active?'ACTIVE':'INACTIVE' }}</span></div><h2 class="mt-4 text-sm font-extrabold">{{ $hospital->name }}</h2><p class="mt-1 text-[9px] text-slate-400">{{ $hospital->businessUnit->name }} · {{ $hospital->city }}</p><div class="mt-4 grid gap-2 text-[9px] text-slate-500"><p class="flex items-center gap-2"><i data-lucide="user-round" class="size-3.5"></i>{{ $hospital->primary_contact_name ?: 'No primary contact' }}</p><p class="flex items-center gap-2"><i data-lucide="map-pin" class="size-3.5"></i><span class="line-clamp-1">{{ $hospital->address ?: 'Address not added' }}</span></p></div>@if($tmsUser->isRole('GM','AM'))<details class="mt-4 border-t border-slate-100 pt-3"><summary class="cursor-pointer text-[9px] font-bold text-blue-600">Edit details</summary><form method="POST" action="{{ route('tms.hospitals.update',$hospital) }}" class="mt-3 grid gap-2">@csrf @method('PATCH')<input name="name" value="{{ $hospital->name }}" required class="h-9 rounded-lg border-slate-200 text-[10px]"><select name="business_unit_id" class="h-9 rounded-lg border-slate-200 text-[10px]">@foreach($businessUnits as $unit)<option value="{{ $unit->id }}" @selected($hospital->business_unit_id===$unit->id)>{{ $unit->name }}</option>@endforeach</select><input name="city" value="{{ $hospital->city }}" required class="h-9 rounded-lg border-slate-200 text-[10px]"><input name="primary_contact_name" value="{{ $hospital->primary_contact_name }}" placeholder="Primary contact" class="h-9 rounded-lg border-slate-200 text-[10px]"><input type="hidden" name="is_active" value="{{ (int)$hospital->is_active }}"><button class="h-9 rounded-lg bg-slate-950 text-[9px] font-bold text-white">Save changes</button></form></details>@endif</article>@empty<div class="col-span-full rounded-2xl border border-dashed border-slate-300 py-16 text-center text-xs text-slate-400">No hospitals match this view.</div>@endforelse</div><div class="mt-4">{{ $hospitals->links() }}</div>
<div x-show="createOpen" x-cloak class="fixed inset-0 z-[70] flex items-end justify-center bg-slate-950/45 sm:items-center sm:p-5" @click.self="createOpen=false"><form method="POST" action="{{ route('tms.hospitals.store') }}" class="max-h-[92vh] w-full max-w-xl overflow-y-auto rounded-t-2xl bg-white p-5 shadow-2xl sm:rounded-2xl">@csrf<div class="flex items-start justify-between"><div><h2 class="text-lg font-extrabold">Create hospital</h2><p class="mt-1 text-[10px] text-slate-500">Add an account to the shared directory.</p></div><button type="button" @click="createOpen=false" class="grid size-9 place-items-center rounded-lg bg-slate-100"><i data-lucide="x" class="size-4"></i></button></div><div class="mt-5 grid gap-4 sm:grid-cols-2"><label class="grid gap-1 text-[10px] font-bold sm:col-span-2">Hospital name<input name="name" required class="h-11 rounded-xl border-slate-200 text-xs"></label><label class="grid gap-1 text-[10px] font-bold">Business unit<select name="business_unit_id" required class="h-11 rounded-xl border-slate-200 text-xs">@foreach($businessUnits as $unit)<option value="{{ $unit->id }}">{{ $unit->name }}</option>@endforeach</select></label><label class="grid gap-1 text-[10px] font-bold">City<input name="city" required class="h-11 rounded-xl border-slate-200 text-xs"></label><label class="grid gap-1 text-[10px] font-bold sm:col-span-2">Address<textarea name="address" rows="2" class="rounded-xl border-slate-200 text-xs"></textarea></label><label class="grid gap-1 text-[10px] font-bold">Contact name<input name="primary_contact_name" class="h-11 rounded-xl border-slate-200 text-xs"></label><label class="grid gap-1 text-[10px] font-bold">Designation<input name="primary_contact_designation" class="h-11 rounded-xl border-slate-200 text-xs"></label><label class="grid gap-1 text-[10px] font-bold">Phone<input name="phone" class="h-11 rounded-xl border-slate-200 text-xs"></label><label class="grid gap-1 text-[10px] font-bold">Email<input type="email" name="email" class="h-11 rounded-xl border-slate-200 text-xs"></label><input type="hidden" name="is_active" value="1"></div><button class="mt-5 h-11 w-full rounded-xl bg-blue-600 text-xs font-bold text-white">Create hospital</button></form></div></div>
@endsection
