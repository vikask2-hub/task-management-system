@extends('layouts.tms')
@section('title','Team')
@section('content')
<div x-data="{ createOpen: false }"><div class="flex items-end justify-between"><div><p class="text-[10px] font-bold tracking-[.15em] text-blue-600 uppercase">Organisation</p><h1 class="mt-1 text-2xl font-extrabold tracking-[-.045em] sm:text-3xl">Team</h1><p class="mt-1 text-xs text-slate-500">Role, reporting line, and operating scope.</p></div>@if($tmsUser->isRole('GM'))<button @click="createOpen=true" class="inline-flex min-h-11 items-center gap-2 rounded-xl bg-blue-600 px-4 text-xs font-bold text-white"><i data-lucide="user-round-plus" class="size-4"></i>Create user</button>@endif</div>
<div class="mt-5 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div class="hidden grid-cols-[1.2fr_.45fr_.8fr_.8fr_.5fr] gap-4 border-b border-slate-100 bg-slate-50 px-5 py-3 text-[8px] font-bold text-slate-400 uppercase lg:grid"><span>Person</span><span>Role</span><span>Business unit</span><span>Manager</span><span>Status</span></div><div class="divide-y divide-slate-100">@foreach($users as $user)<div class="grid gap-3 px-5 py-4 lg:grid-cols-[1.2fr_.45fr_.8fr_.8fr_.5fr] lg:items-center"><div class="flex items-center gap-3"><span class="grid size-9 place-items-center rounded-xl text-[9px] font-bold text-white" style="background:{{ $user->avatar_color }}">{{ collect(explode(' ',$user->name))->map(fn($n)=>mb_substr($n,0,1))->take(2)->join('') }}</span><div><p class="text-[11px] font-bold">{{ $user->name }}</p><p class="mt-0.5 text-[8px] text-slate-400">{{ $user->email }} · {{ $user->employee_code }}</p></div></div><span class="w-fit rounded-full bg-blue-50 px-2.5 py-1 text-[8px] font-bold text-blue-700">{{ $user->role }}</span><p class="text-[9px] text-slate-500">{{ $user->defaultBusinessUnit?->name ?? 'Pan India' }}</p><p class="text-[9px] text-slate-500">{{ $user->manager?->name ?? '—' }}</p><span class="text-[8px] font-bold {{ $user->is_active?'text-emerald-600':'text-rose-600' }}">{{ $user->is_active?'ACTIVE':'INACTIVE' }}</span></div>@endforeach</div></div><div class="mt-4">{{ $users->links() }}</div>
@if($tmsUser->isRole('GM'))<div x-show="createOpen" x-cloak class="fixed inset-0 z-[70] flex items-end justify-center bg-slate-950/45 sm:items-center sm:p-5" @click.self="createOpen=false"><form method="POST" action="{{ route('tms.team.store') }}" class="max-h-[92vh] w-full max-w-xl overflow-y-auto rounded-t-2xl bg-white p-5 sm:rounded-2xl">@csrf<div class="flex justify-between"><div><h2 class="text-lg font-extrabold">Create team member</h2><p class="mt-1 text-[10px] text-slate-500">Set the role and reporting scope.</p></div><button type="button" @click="createOpen=false" class="grid size-9 place-items-center rounded-lg bg-slate-100"><i data-lucide="x" class="size-4"></i></button></div><div class="mt-5 grid gap-4 sm:grid-cols-2"><label class="grid gap-1 text-[10px] font-bold sm:col-span-2">Full name<input name="name" required class="h-11 rounded-xl border-slate-200 text-xs"></label><label class="grid gap-1 text-[10px] font-bold">Email<input type="email" name="email" required class="h-11 rounded-xl border-slate-200 text-xs"></label><label class="grid gap-1 text-[10px] font-bold">Employee code<input name="employee_code" class="h-11 rounded-xl border-slate-200 text-xs"></label><label class="grid gap-1 text-[10px] font-bold">Role<select name="role" class="h-11 rounded-xl border-slate-200 text-xs">@foreach(['GM','AM','BDE'] as $role)<option>{{ $role }}</option>@endforeach</select></label><label class="grid gap-1 text-[10px] font-bold">Business unit<select name="default_business_unit_id" class="h-11 rounded-xl border-slate-200 text-xs"><option value="">Pan India / none</option>@foreach($businessUnits as $unit)<option value="{{ $unit->id }}">{{ $unit->name }}</option>@endforeach</select></label><label class="grid gap-1 text-[10px] font-bold">Reporting manager<select name="direct_manager_id" class="h-11 rounded-xl border-slate-200 text-xs"><option value="">None</option>@foreach($managers as $manager)<option value="{{ $manager->id }}">{{ $manager->name }}</option>@endforeach</select></label><label class="grid gap-1 text-[10px] font-bold">Temporary password<input type="password" name="password" required minlength="8" class="h-11 rounded-xl border-slate-200 text-xs"></label><input type="hidden" name="is_active" value="1"></div><button class="mt-5 h-11 w-full rounded-xl bg-blue-600 text-xs font-bold text-white">Create user</button></form></div>@endif</div>
@endsection
