@extends('layouts.tms')
@section('title','Notifications')
@section('content')
<div><p class="text-[10px] font-bold tracking-[.15em] text-blue-600 uppercase">Attention centre</p><h1 class="mt-1 text-2xl font-extrabold tracking-[-.045em] sm:text-3xl">Notifications</h1><p class="mt-1 text-xs text-slate-500">Assignments, verification, rework, and manager updates.</p></div><section class="mt-6 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div class="divide-y divide-slate-100">@forelse($notifications as $notification)<div class="flex items-start gap-3 p-4 sm:p-5 {{ $notification->is_read?'':'bg-blue-50/40' }}"><span class="grid size-10 shrink-0 place-items-center rounded-xl {{ $notification->is_read?'bg-slate-100 text-slate-500':'bg-blue-600 text-white' }}"><i data-lucide="bell-ring" class="size-4"></i></span><div class="min-w-0 flex-1"><div class="flex flex-wrap items-center gap-2"><p class="text-[11px] font-bold">{{ $notification->title }}</p>@unless($notification->is_read)<span class="rounded-full bg-blue-600 px-2 py-0.5 text-[7px] font-bold text-white">NEW</span>@endunless</div><p class="mt-1 text-[10px] leading-4 text-slate-500">{{ $notification->body }}</p><p class="mt-1.5 text-[8px] text-slate-400">{{ $notification->created_at->diffForHumans() }}</p></div>@unless($notification->is_read)<form method="POST" action="{{ route('tms.notifications.read',$notification) }}">@csrf @method('PATCH')<button class="min-h-10 rounded-xl border border-slate-200 px-3 text-[8px] font-bold">Mark read</button></form>@endunless</div>@empty<div class="py-16 text-center text-xs text-slate-400">You’re all caught up.</div>@endforelse</div></section><div class="mt-4">{{ $notifications->links() }}</div>
@endsection
