@extends('layouts.tms')
@section('title','Recurring tasks')
@section('content')
<div x-data="{ createOpen:false }"><div class="flex items-end justify-between"><div><p class="text-[10px] font-bold tracking-[.15em] text-blue-600 uppercase">Automation</p><h1 class="mt-1 text-2xl font-extrabold tracking-[-.045em] sm:text-3xl">Recurring tasks</h1><p class="mt-1 text-xs text-slate-500">Generate consistent daily and weekly execution.</p></div><button @click="createOpen=true" class="inline-flex min-h-11 items-center gap-2 rounded-xl bg-blue-600 px-4 text-xs font-bold text-white"><i data-lucide="plus" class="size-4"></i>Create recurring</button></div>
<div class="mt-6 grid gap-3 lg:grid-cols-2">@forelse($templates as $template)<article class="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div class="flex items-start justify-between"><span class="grid size-10 place-items-center rounded-xl bg-blue-50 text-blue-700"><i data-lucide="refresh-cw" class="size-4"></i></span><span class="rounded-full px-2.5 py-1 text-[8px] font-bold {{ $template->is_active?'bg-emerald-50 text-emerald-700':'bg-slate-100 text-slate-500' }}">{{ $template->is_active?'ACTIVE':'PAUSED' }}</span></div><p class="mt-4 text-[9px] font-bold tracking-wide text-blue-600 uppercase">{{ str($template->schedule_type)->replace('_',' ') }}</p><h2 class="mt-1 text-sm font-extrabold">{{ $template->name }}</h2><p class="mt-1 text-[10px] text-slate-500">{{ $template->title }}</p><div class="mt-4 grid grid-cols-3 gap-2 rounded-xl bg-slate-50 p-3 text-center"><div><p class="text-[8px] text-slate-400">Assignee</p><p class="mt-1 truncate text-[9px] font-bold">{{ $template->assignee->name }}</p></div><div><p class="text-[8px] text-slate-400">Due</p><p class="mt-1 text-[9px] font-bold">{{ \Carbon\Carbon::parse($template->preferred_due_time)->format('g:i A') }}</p></div><div><p class="text-[8px] text-slate-400">Last run</p><p class="mt-1 text-[9px] font-bold">{{ $template->last_generated_for_date?->format('d M')??'—' }}</p></div></div><div class="mt-4 flex gap-2"><form method="POST" action="{{ route('tms.recurring.generate',$template) }}" class="flex-1">@csrf<button class="h-10 w-full rounded-xl bg-slate-950 text-[9px] font-bold text-white">Generate today</button></form><form method="POST" action="{{ route('tms.recurring.destroy',$template) }}">@csrf @method('DELETE')<button class="h-10 rounded-xl border border-slate-200 px-4 text-[9px] font-bold text-slate-600">{{ $template->is_active?'Pause':'Resume' }}</button></form></div></article>@empty<div class="col-span-full rounded-2xl border border-dashed border-slate-300 py-16 text-center text-xs text-slate-400">No recurring tasks yet.</div>@endforelse</div><div class="mt-4">{{ $templates->links() }}</div>
<div x-show="createOpen" x-cloak class="fixed inset-0 z-[70] flex items-end justify-center bg-slate-950/45 sm:items-center sm:p-5" @click.self="createOpen=false"><form method="POST" action="{{ route('tms.recurring.store') }}" class="max-h-[92vh] w-full max-w-2xl overflow-y-auto rounded-t-2xl bg-white p-5 sm:rounded-2xl">@csrf<div class="flex justify-between"><div><h2 class="text-lg font-extrabold">Create recurring task</h2><p class="mt-1 text-[10px] text-slate-500">Define one repeatable assignment template.</p></div><button type="button" @click="createOpen=false" class="grid size-9 place-items-center rounded-lg bg-slate-100"><i data-lucide="x" class="size-4"></i></button></div><div class="mt-5 grid gap-4 sm:grid-cols-2"><label class="grid gap-1 text-[10px] font-bold">Template name<input name="name" required class="h-11 rounded-xl border-slate-200 text-xs"></label><label class="grid gap-1 text-[10px] font-bold">Task title<input name="title" required class="h-11 rounded-xl border-slate-200 text-xs"></label><label class="grid gap-1 text-[10px] font-bold">Category<select name="category_id" class="h-11 rounded-xl border-slate-200 text-xs">@foreach($categories as $category)<option value="{{ $category->id }}">{{ $category->name }}</option>@endforeach</select></label><label class="grid gap-1 text-[10px] font-bold">Priority<select name="priority" class="h-11 rounded-xl border-slate-200 text-xs">@foreach(['LOW','MEDIUM','HIGH','URGENT'] as $priority)<option>{{ $priority }}</option>@endforeach</select></label><label class="grid gap-1 text-[10px] font-bold">Business unit<select name="business_unit_id" class="h-11 rounded-xl border-slate-200 text-xs">@foreach($businessUnits as $unit)<option value="{{ $unit->id }}">{{ $unit->name }}</option>@endforeach</select></label><label class="grid gap-1 text-[10px] font-bold">Assignee<select name="assignee_id" class="h-11 rounded-xl border-slate-200 text-xs">@foreach($assignees as $assignee)<option value="{{ $assignee->id }}">{{ $assignee->name }}</option>@endforeach</select></label><label class="grid gap-1 text-[10px] font-bold">Frequency<select name="schedule_type" class="h-11 rounded-xl border-slate-200 text-xs">@foreach(['DAILY','WEEKDAYS','WEEKLY','MONTHLY'] as $frequency)<option>{{ $frequency }}</option>@endforeach</select></label><label class="grid gap-1 text-[10px] font-bold">Preferred due time<input type="time" name="preferred_due_time" value="17:30" required class="h-11 rounded-xl border-slate-200 text-xs"></label><label class="grid gap-1 text-[10px] font-bold">Start date<input type="date" name="start_date" value="{{ now()->toDateString() }}" required class="h-11 rounded-xl border-slate-200 text-xs"></label><label class="grid gap-1 text-[10px] font-bold">End date<input type="date" name="end_date" class="h-11 rounded-xl border-slate-200 text-xs"></label><label class="flex min-h-11 items-center gap-2 rounded-xl border border-slate-200 px-3 text-[10px] font-bold"><input type="hidden" name="verification_required" value="0"><input type="checkbox" name="verification_required" value="1" checked>Verification required</label><input type="hidden" name="is_active" value="1"></div><button class="mt-5 h-11 w-full rounded-xl bg-blue-600 text-xs font-bold text-white">Create recurring task</button></form></div></div>
@endsection
